# Implementation Plan: Time, Consent, Instructions, and Admin Toggles

## Task 1: Change default time for conditions to 5 minutes
- Update `src/Admin.tsx` to set default `timerMinutes` to 5 instead of 10.
- Update `src/Feed.tsx` to use `5 * 60` for the fallback `setTimeLeft` instead of `10 * 60`.

## Task 2: Ensure consent form shows up every time when returning to the landing page
- In `src/Landing.tsx`, inside `useEffect`, call `localStorage.removeItem('feed-consent-given')`.
- Also in `src/Landing.tsx`, if we want the App to immediately pick up the change and show the consent form without a page reload, we need `App.tsx` and `Landing.tsx` to communicate, or simpler: since `App.tsx` handles rendering `InformedConsent` based on its own state `consented`, we can just change `App.tsx` to not wrap `Landing` with the consent form check. Wait, if `App` wraps everything, `Landing` won't render if `!consented`. So we must change `App.tsx`.
- In `App.tsx`: Move the `InformedConsent` check into a wrapper component for routes that require consent (like `/feed`, `/feed/a`, `/feed/b`), OR change the `App.tsx` logic so the consent state is reset when the path is `/`. Using `window.location.pathname` is tricky. Alternatively, have a `RequireConsent` component that wraps the Feed routes, and remove the global `!consented` check from `App.tsx`.
- Let's use a `RequireConsent` wrapper in `App.tsx` around the `/feed*` routes. Wait, `InformedConsent` replaces the whole screen content. Let's make a `RequireConsent` component inside `App.tsx` that checks local storage, and if not present, renders `InformedConsent`, else renders its `.children`. 

## Task 3: Create small instructions on the landing page
- Add instructions text in `src/Landing.tsx` below the title or buttons. 
- Example text: "Instructions: Select your assigned condition. Scroll through the videos until the timer finishes, then proceed to the final survey."
- Ensure these instructions do not reveal the experiment mechanics (just navigation rules). 

## Task 4: Allow toggling conditions on/off in the admin dashboard
- Update `AdminSettings` in `src/Admin.tsx` to include `showConditionA: boolean` (default true) and `showConditionB: boolean` (default true).
- Add toggles (checkboxes) in `src/Admin.tsx` for these.
- Update `src/Landing.tsx` to read these settings and hide the respective condition buttons if they form the fallback view. (If `settings.feedName` is set, it shows the single entry button. We should also allow toggling the fallback condition A and B buttons if `settings.feedName` is empty).
